import { createTheme } from "@mui/material/styles";

const theme = createTheme({

    palette: {
        yellow: '#FECB5D',
        saffron:"#F28A1E",
        headerButton: "#E82B09",
    },
});

export default theme